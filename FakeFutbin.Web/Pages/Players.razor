@page "/"
@inherits PlayersBase

@if (Players == null)
{
    <h3>Loading Players...</h3>
}
else
{
    <h3 class="mb-5">Players</h3>
    var playerCount = Players.Count();

    @if(playerCount > 0)
    {
        @foreach (var playerGroup in GetGroupedPlayersByNationality())
        {
            <h4>@GetNationalityName(playerGroup)</h4>
            <div class="row mt-3">
                <DisplayPlayer Players = "@playerGroup.Take(4)"></DisplayPlayer>
            </div>
            <hr class="mb-3">
        }
    }
}