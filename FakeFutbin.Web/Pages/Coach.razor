@page "/Coach"
@inherits CoachBase

@if (CoachPlayers == null && ErrorMessage == null)
{
    <DisplaySpinner></DisplaySpinner>
}
else if(ErrorMessage != null)
{
    <DisplayError ErrorMessage="@ErrorMessage"></DisplayError>
}
else
{
    <h3 class="mb-5"><b>Coach</b></h3>
    @if (CoachPlayers.Count() > 0)
    {
    <div class = "row mb-5">
        <div class ="col-md-9">
                @foreach (var player in CoachPlayers)
            {
                <div class="row mb-4">
                    <div class="col-md-4">
                        <img src="@player.PlayerImageURL" width="300" class="img-thumbnail">
                    </div>
                    <div class = "col-md-8">
                        <h5><b>@player.PlayerName</b></h5>
                        <div class="mb-4">Age: @player.PlayerAge</div>
                        <div class="mb-4"> Raiting: @player.PlayerRaiting</div>
                        <span>
                            Market Value: <b>@player.MarketValue.ToString("C")</b>
                            <input @oninput ="()=>UpdateQty_Input(player.Id)"
                            type="number" @bind = "@player.Qty"/>
                            <button data-playerId="@player.Id" class="btn btn-info btn-sm update-qty"
                            @onclick ="(()=> UpdateQtyCoachPlayer_Click(player.Id, player.Qty))"
                            >Update Qty</button>
                        </span>
                        <div>
                            <button @onclick ="(() => DeleteCoachPlayer_Click(player.Id))"
                            class="btn btn-danger sm"><i class="fa fa-trash"></i></button>
                        </div>
                    </div>
                </div>
            }
        </div>
        <div class="col-md-3">
            <h5>Coach Summary</h5>
            <div class="mt-2">
                <div>Total - (@TotalQuantity players)&nbsp<b>@TotalValue</b>;</div>
                <a href="#" class="btn btn-success">
                    <span class="fa fa-check"></span>&nbsp; Proceed to Checkout 
                </a>
            </div>
        </div>
    </div>
    }
    else
    {
        <h4>There are currently no players in your squad!</h4>
    }

}
